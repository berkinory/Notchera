# notchera 0.1.1

## improvements

- brew update instructions in settings now use `brew update && brew upgrade --cask notchera` to avoid stale tap metadata issues.

## bug fixes

- fixed brew update checks to use Homebrew directly instead of the Sparkle appcast.
- removed the incorrect version label from the menubar menu.
